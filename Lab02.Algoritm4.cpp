#include <iostream>
#include <math.h>
using namespace std;

main(){
    double a, b;
    cout << "Введите a: ";
    cin >> a;
    cout << "Введите b: ";
    cin >> b;
    cout << ((a + b < 20) ? "Ну типо мало" : "Типо много"); 

return 0; }